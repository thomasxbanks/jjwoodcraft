{% block html_head_container %}

	{% include 'html-header.twig' %}
	{% block head %}{% endblock %}
</head>{% endblock %}<body class="{{body_class}}" data-template="base.twig" data-title="{% if wp_title %} {{ wp_title }} {% else %} {{ site.name }} {% endif %}">

{% include 'masthead.twig' %}

<main>
	{% block content %}
		Sorry, no content
	{% endblock %}
</main>

{% block footer %}
	{% include 'footer.twig' %}
	{{ function('wp_footer') }}
{% endblock %}
<script src="https://cdn.jsdelivr.net/npm/macy@2"></script></body></html>
